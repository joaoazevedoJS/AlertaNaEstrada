import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  section: {
    alignItems: "center",
    marginVertical: 32, 
  },

  title: {
    fontSize: 24,
    fontFamily: "OpenSans_600SemiBold",
    color: "#ddd",
  }
 })